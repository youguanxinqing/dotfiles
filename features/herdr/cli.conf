# platform | id | command | installer | source
#
# Herdr 的插件。script installer 的 command 字段用不上（cli_is_installed 对 script
# 只跑 "<source> check"），这里填 herdr 只是表明依赖谁。
all | herdr-plugins | herdr | script | features/herdr/install-plugins.sh
