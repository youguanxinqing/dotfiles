[module]
platforms = macos
enabled = true
order = 50

# 装 app（缺了就走 cask）并把 subl 链到 ~/.local/bin，见 install.sh 里的原因。
[dependency sublime-text]
platform = macos
command = subl
installer = script
source = install.sh
