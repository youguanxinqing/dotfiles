[module]
platforms = all
enabled = true
order = 50

# Codex 会把项目信任、插件注册、hook hash 等本机状态写进
# ~/.codex/config.toml，不能把那个文件直接链接进公开仓库。
# 命名 profile 是 Codex 原生的配置层：基础文件继续归 Codex 管，
# 这里的人工偏好只读叠加，并由 Fish 的 codex 函数默认启用。

